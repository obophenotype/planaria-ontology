pattern_name: plana_terms 
pattern_iri: https://tbd
description: "simple terms unique to PLANA"

contributors:
  - https://orcid.org/0000-0002-3528-5267

classes:
  edit_entity: BFO:0000040

annotationProperties:
  depicted_by: foaf:depicted_by
  image_comment: rdfs:comment
  term_id: oboInOwl:id

vars:
  edit_entity: "'new_entity'"
  depicted_by: "'depicted_by'"
  image_comment: "'image_comment'"

data_list_vars:
  dbxref: xsd:string
  def_dbxref: xsd:string
  exact_syn: xsd:string

annotations:
  
  - annotationProperty: depicted_by
    var: depicted_by
    annotations:
      - annotationProperty: image_comment
        text: "%s"
        vars:
         - image_comment

name:
  text: "%s"
  vars:
    - edit_entity

exact_synonym:
    value: exact_syn

xref:
  value: dbxref

def:
  text: "%s needs def"
  vars:
    - edit_entity
  xrefs: def_dbxref


